[... old obsolete project statement]

right now, you, as claude code, need to work with me to decide on a strategy to port this codebase from a position of extreme literacy and enlightenment. *you* can either read these essays and this user input and *understand* what we are trying to do, *or* you can read the reference code, which is problematic specifically because it is too bloated, and become unable to understand why you were reading it after drinking a few too many thousands of tokens down all at once.
*you*, as the root claude code session, probably need to *not read* the code these reports are about. and to use, for example, subagents (not necessarily grander subagents than sonnets...) to work from directives and rubrics for mapping and rewriting code to new testable replications instead. again, there is a painful tradeoff between *literacy* and *action*. if the root session tries to take actions themselves, they will become incapable of reviewing the results they are drinking down, drowning from. so the root session can't do that, and must exercise extreme restraint in *only* using subagents to read, to write, to synthesize reports, and, as the presiding over-agent, write their own essays in response to every 2-3 essays they receive as results from *subagents* tasked with *writing reviews of recent actions*. remember that you just can't read any of the literal files this stuff is about for now (sorry), besides essays of the format i have just suggested, until the burden of careful reimplementation of the core program features (and their total recomposition and refactoring to the simplest form which supports the mvp optimal inference and training DNN launches we actually need, sorry, there's no room for 'play' implementations which step geneaologically forwards from bad ugly slow implementations towards correct+fast ones. that already happened; we can't get from 'lore' to 'good' by incremental steps; a discontinuity is needed *and this is that discontinuity*.) has been taken off of our shoulders. fortunately the writing here carefully outlines what constitutes replication, and this can be confirmed by tests rather than micromanagement and reading too many things.